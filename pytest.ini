[pytest]
testpaths = tests
markers =
    smoke: gercek Foundry Local modelleri gerektiren uctan uca testler
; smoke testleri varsayilan calistirmada atlanir; "-m smoke" ile calistirin
addopts = -m "not smoke"
